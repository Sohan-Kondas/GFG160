//Number of occurrence
class Solution:
    def countFreq(self, arr, target):
        #code here
        res=0
        for i in range(len(arr)):
            if arr[i]==target:
                res+=1
                
        return res
